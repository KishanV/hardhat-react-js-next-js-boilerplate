import styled from "styled-components";
import { SingleElement } from "../utils/single-elemet";

export const Flex = SingleElement({
  className: "sp-flex",
});

export const Fill = SingleElement({
  className: "sp-fill",
});

export const Box = styled.div``;
